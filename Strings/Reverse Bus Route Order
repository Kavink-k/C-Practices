#include <stdio.h>
int main() {
    int arr[100],arr1[100],i,n,k=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)scanf("%d",&arr[i]);
    
    for(i=n-1;i>=0;i--){
        arr1[k++]=arr[i];
    }
for(i=0;i<k;i++){
    printf("%d ",arr1[i]);
}
    return 0;
}
