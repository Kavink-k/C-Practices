#include <stdio.h>
int main() {
    int arr[100],i,j,n,m;
    scanf("%d",&n);
    for(i=0;i<n;i++)scanf("%d",&arr[i]);
    scanf("%d",&m);
    int count=0;
    for(i=0;i<n;i++){
        if(arr[i]==m){
            count++;
        }
    }
    printf("%d",count);

    return 0;
}
