#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdbool.h>


int main() {
    int i,j,arr1[100],arr2[100],n1,n2,arr[100];
    scanf(" %d",&n1);
    bool visited[n1];
    for(i=0;i<n1;i++) visited[i]=false;
    for(i=0;i<n1;i++) scanf("%d",&arr1[i]);
    scanf("%d",&n2);
    for(i=0;i<n2;i++) scanf("%d",&arr2[i]);
    int k=0;
    if(n1==n2){
    for(i=0;i<n1;i++){
        if(visited[i]==true) continue;
        for(j=0;j<n2;j++){
            if(arr1[i]==arr2[j]){
                visited[i]=true;
                arr[k++]=arr1[i];
            }
        }
    }
    }else{printf("Enter Equal number of string size");}

    for(i=0;i<k;i++){
        printf("%d ",arr[i]);
    }
    return 0;
}
